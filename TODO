- multi-line parsing

- print a message when the result of a method is null

- “bootstrap” configuration entry which indicates which PHP file to load before
  Tequila initialization.

- improve and test the “teq” command.

- In case of PHP errors, logs it and displays a short explanation to the user.

- Should we use “var_export()” to display the returned result of commands?
